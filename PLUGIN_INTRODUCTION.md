# Fire Project 自研插件功能简介

本项目包含四个自主研发的核心工具包，分别针对**VR 开发效率、编辑器性能、AI 交互探索、版本协作**提出了创新性的解决方案。

---

## 1. Fire VR Converter (VR 项目一键转换器)

### 📋 功能简介
Fire VR Converter 致力于解决“将普通 3D 项目升级为 VR 项目”时繁琐的手动配置痛点。它提供了一个“一键式”的向导流程，能够自动分析当前项目环境，并完成从依赖安装到场景搭建的全过程。

### 💻 技术点
- **Package Manager 自动化**: 通过代码动态操作 `Client` API，自动检索并安装 `XR Interaction Toolkit`, `OpenXR Plugin`, `XR Plugin Management` 等核心依赖包。
- **XR 环境配置**: 自动生成并配置 `XR General Settings` 和 `XR Manager Settings` ScriptableObject，免去开发者手动调整 Project Settings 的困扰。
- **场景重构**: 智能分析当前场景的主摄像机（Main Camera），将其自动升级为标准的 XR Origin (VR Rig) 结构，并自动挂载左右手控制器与交互管理器。

### 💡 创新点
- **零门槛升级**: 将需耗时数十分钟的专业配置流程压缩为“一次点击”，极大幅度降低了传统 3D 开发者尝试 VR 开发的门槛。
- **智能依赖解析**: 能够识别当前 Unity 版本并自动匹配最兼容的 XR 插件版本，避免版本冲突。

---

## 2. Focus Optimizer (焦点卡顿优化助手)

### 📋 功能简介
针对 Unity 编辑器在切换窗口（失去/获取焦点）时频繁触发 `AssetDatabase.Refresh` 导致卡顿的问题，Focus Optimizer 接管了编辑器的资源刷新机制。它在编辑器后台运行时暂停刷新，在切回前台时根据文件变动情况智能执行刷新策略。

### 💻 技术点
- **AssetDatabase 接管**: 利用 `AssetDatabase.DisallowAutoRefresh()` 和 `AllowAutoRefresh()` API 彻底接管 Unity 原生的自动刷新行为。
- **独立文件监控**: 引入 `System.IO.FileSystemWatcher`，在 Unity 暂停刷新期间独立监控 `Assets` 和 `Packages` 目录的文件变动。
- **多线程缓冲**: 使用独立线程处理文件系统事件，并通过线程安全的缓冲区记录变动路径，避免阻塞主编辑器线程。

### 💡 创新点
- **“监控-拦截-决策”架构**: 并非简单地屏蔽刷新，而是通过独立监控层掌握变动详情。如果变动量巨大（如 Git 切换分支），它会智能拦截并询问用户，防止编辑器直接卡死。
- **无感介入**: 对用户完全透明，安装即生效，显著提升了开发过程中频繁查阅文档、操作外部工具时的流畅度。

---

## 3. Pose Drive (AI 姿态驱动系统)

### 📋 功能简介
Pose Drive 是一个基于普通摄像头的 AI 体感输入系统。它允许开发者在**没有 VR 头显和手柄**的情况下，仅通过 PC/Mac 摄像头捕捉身体和手部动作，直接驱动 VR 场景中的交互逻辑。

### 💻 技术点
- **端侧 AI 推理**: 集成 **Unity Barracuda** 推理引擎，直接在本地运行 Google **MoveNet** 轻量级姿态估计模型，无需联网。
- **虚拟输入映射**: 封装了一套虚拟 Input System，将 AI 识别到的头部旋转、身体移动、手势动作（如挥手、抓取）映射为标准的 Unity Input 信号。
- **动作分类器**: 内置基于关键点的动作分类器（Action Classifier），支持识别特定的交互手势。

### 💡 创新点
- **硬件解耦**: 打破了 VR 开发必须依赖昂贵硬件的限制，让开发者随时随地（甚至在咖啡厅）都能调试 VR 交互逻辑。
- **交互民主化**: 为失语症治疗等场景提供了非穿戴式的交互方案，患者无需佩戴沉重的设备即可通过肢体动作参与康复训练。

---

## 4. Version Control Assistant (版本控制助手)

### 📋 功能简介
这是一个深度集成在 Unity Editor 内部的 Git 图形化客户端。它提供了一个类似 IDE 的文件树状视图，让开发者无需离开 Unity 即可完成查看状态、提交代码、推送/拉取等标准 Git 操作。

### 💻 技术点
- **CLI 封装**: 不依赖庞大的 `libgit2` 或 dll 库，而是直接封装系统的 Git 命令行接口 (CLI)。通过 `System.Diagnostics.Process` 高效执行命令并捕获输出。
- **健壮的编码处理**: 显式处理 UTF-8 编码流，完美解决了 Git 在中文、日文等多语言环境下的乱码问题。
- **状态解析算法**: 解析 `git status --porcelain` 的标准化输出，构建出精确的文件变动树。

### 💡 创新点
- **轻量化集成**: 相比于动辄几百 MB 的第三方 Git 插件，本工具体积极小，且利用了系统已有的 Git 环境。
- **沉浸式体验**: 所有的 UI 设计（包括文件树、差异对比）都完全遵循 Unity 原生风格，提供了无缝的使用体验。
